
export interface ContentBody {
  /**
   * Method arguments
   */
  "&": object;

  /**
   * Instance map (links)
   * This is an object containing all members selected by the server
   */
  "%": object;
}
